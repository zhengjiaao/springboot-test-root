server:
  port: 8080
  servlet:
    context-path: /

spring:
  servlet:
    multipart:
      # 设置总上传数据总大小 5G
      max-request-size: 5121MB
      # 设置单文件大小 2G
      max-file-size: 2049MB
test:
  file:
    db-type: hdfs
    hdfs:
      uri: hdfs://192.168.200.154:8020
      username: hdfs
      path: /ds
