server:
  port: 8080
  servlet:
    context-path: /

spring:
  servlet:
    multipart:
      max-file-size: 2049MB     # 设置单文件大小 2G
      max-request-size: 5121MB  # 设置总上传数据总大小 5G
  # 单机版配置
  redis:
    host: 127.0.0.1
    port: 6379
    password:  # 可选的，默认空
    database: 0